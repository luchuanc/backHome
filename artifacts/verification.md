# 交付验证记录

日期：2026-09-22。独立单机浏览器游戏，无外部运行依赖。

## 自动验证

`npm test`：21 / 21 通过。覆盖地图可达性、固定种子重放、四种武器、墙体遮挡和碰撞、冲刺无敌、远程敌人与 Boss 重复攻击、贴脸命中、分裂弹不递归爆炸、开箱与容量、升级候选、Boss 六分钟门槛、三分钟/五分钟补给、成功与失败结算、快照恢复与拒绝损坏数据、设施购买和持久化错误处理。

完整远征测试只通过 `update` 的移动、冲刺、治疗和交互输入，以及 `chooseUpgrade` 选择升级推进，不修改角色生命或跳过时间。默认装备 seed 17 在 542.4 秒击败守卫、拾取并撤离。设施二级 seed 6/8/10 分别在 423.2 / 588.8 / 424.4 秒完成。同批默认装备的其他种子存在死亡或风暴失败，测试结果不代表无条件通关。

`npm run check` 通过。最终渲染/UI 调整后再次通过对应 `node --check`。

`npm run build` 成功生成独立 HTML，所有源脚本和两个样式文件均内嵌，场景美术以 data URL 内嵌。

## 实际浏览器验证

使用 Codex 浏览器，通过本机 HTTP 打开源码入口和独立 HTML：

- 主菜单 → 营地 → 点击设施 → 购买并扣除正确资源；装备选择与解锁入口。
- 真实右键寻路，自动攻击、敌人命中、掉落、拾取、满包提示。
- 点击交互提示持续开箱，获得废铁与电路；修复了按钮按下时覆盖居中变换、导致点击失效的问题。
- 拾取经验触发 LV.2 三选一，选择钨钢弹芯后恢复战斗。
- 打开全图暂停战斗，关闭恢复；地图在 1280×800 内显示完整图例。
- 保存远征 → 回营地 → 刷新 → 恢复：LV.2、24 击破、30/30 背包（24 废铁、3 电路、1 核心）保留。
- 实际成功撤离：一次带回 26 废铁、4 电路；再次用鼠标持续撤离，带回 24 废铁、3 电路、1 核心。仓库与日志正确更新。
- 实际死亡结算保留 35% 普通材料；结果弹窗、返回营地正常。
- 390×844 竖屏与 844×390 横屏布局检查；修复短横屏任务面板遮挡灯塔热点。
- 设置、减少动态效果、静音和自动射击入口。发现并修复音乐音量为零时压低音量逻辑短暂抬高音乐的问题。
- 测试过程中浏览器错误/警告日志未发现应用错误。

截图：`camp.png`、`combat.png`、`upgrade.png`、`extraction.png`、`camp-mobile.png`。

## 尚未验收

- 未在实体手机、Safari、Firefox 上做兼容性与持续性能验收。
- 未由真人在浏览器中完整打过六分钟后的 Boss；该阶段由完整模拟输入测试覆盖。
- 未实测 JSON 下载后经系统文件选择器重新导入，存档解析/校验与恢复逻辑已测试。
- 浏览器工具策略阻止 file:// 导航，因此未通过工具验证直接双击打开的路径；同一独立 HTML 已通过 HTTP 正常运行。没有绕过该限制。
- 未做主观音频听感验收。

试玩数据为本次开发测试生成，交付预览前已通过游戏内重置入口清空。
